set mbox = "~/.mail/inbox"
set move = no
set folder = "~/.mail"
set record = +sent
mailboxes +inbox +mail_gmail +mail_163 +mail_yahoo

# Default action:
folder-hook . set from = hang.xiao.hh@gmail.com
folder-hook . set realname = "Hang Xiao"
# First account:
folder-hook mail_gmail set from = hang.xiao.hh@gmail.com
folder-hook mail_gmail set realname = "Hang Xiao"
# Second account:
folder-hook mail_163 set from = my_645@163.com
folder-hook mail_163 set realname = mar
# Third account:
folder-hook mail_yahoo set from = xiaohang_87@yahoo.com.cn
folder-hook mail_yahoo set realname = "Hang Xiao"

alternates "hang\.xiao\.hh@gmail\.com|my_65@163\.com|xiaohang_87@yahoo\.com\.cn"

#===================================================#
# Boring details
set realname = "Hang Xiao"
set from = "hang.xiao.hh@gmail.com"
set use_from = yes 
set envelope_from = yes
set reverse_name = yes

set signature="~/.signature"
set sendmail="/usr/bin/msmtp"
set spoolfile="~/.mail/inbox"

set folder="$HOME/.mail"
set record="+send"
set postponed="+postponed"
set mbox_type=mbox
set move=no

set sort_browser=alpha
ignore *
unignore Date: From: User-Agent: X-Mailer X-Operating-System To: \
         Cc: Reply-To: Subject: Mail-Followup-To:
hdr_order Date: From: User-Agent: X-Mailer X-Operating-System To: \
        Cc: Reply-To: Subject: Mail-Followup-To:

set editor="vim -c 'set tw=70 et' '+/^$' " 
set edit_headers=yes         # See the headers when editing

set alias_file = ~/.mutt/mutt_aliases  # In their own file
source ~/.mutt/mutt_aliases            # Source them
set sort_alias=alias                   # Sort alphabetically

lists mutt-users
subscribe mutt-users
set followup_to=yes        # Sets 'Mail-Followup-To' header
set honor_followup_to=yes  
fcc-hook mutt-user +mutt   # See your own posts using fcc

set markers          # mark wrapped lines of text in the pager with a +
set smart_wrap       # Don't wrap mid-word
set pager_context=5  # Retain 5 lines of previous page when scrolling.
set status_on_top    # Status bar on top.
push <show-version>  # Shows mutt version at startup

set sort=reverse-date-sent        # sort by date reverse

macro index,pager I '<shell-escape> fetchmail <enter>'

